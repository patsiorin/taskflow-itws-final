{
  "name": "taskflow-clean-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Layered ITWS final project monorepo for a Kanban todo app.",
  "engines": {
    "node": ">=20 <23",
    "npm": ">=10"
  },
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently --names backend,frontend,urls --prefix-colors blue,green,magenta \"npm run backend:dev\" \"npm run frontend:dev\" \"npm run dev:urls\"",
    "dev:urls": "node scripts/print-dev-urls.mjs",
    "frontend:dev": "npm run dev -w @taskflow/frontend",
    "frontend:build": "npm run build -w @taskflow/frontend",
    "backend:dev": "npm run start:dev -w @taskflow/backend",
    "backend:build": "npm run build -w @taskflow/backend",
    "db:apply": "npm run db:apply -w @taskflow/backend",
    "prisma:generate": "npm run prisma:generate -w @taskflow/backend",
    "prisma:push": "npm run prisma:push -w @taskflow/backend",
    "prisma:migrate": "npm run prisma:migrate -w @taskflow/backend",
    "build": "npm run backend:build && npm run frontend:build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "prettier": "^3.4.2",
    "typescript": "^5.7.3"
  }
}
